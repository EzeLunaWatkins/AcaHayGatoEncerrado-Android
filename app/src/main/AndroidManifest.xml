<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ui.unq.ezelunawatkins.acahaygatoencerrado">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="service.InventarioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="service.LaberintoSeleccionadoActivity"
            android:label="@string/title_activity_laberinto_seleccionado"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="service.LaberintosDisponiblesActivity"
            android:label="@string/title_activity_laberintos_disponibles"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>